# Runtime dependencies for the Identity Reconciliation service.
# Pinned to minor versions so that the image built by CI today is the same
# image that gets built on a rollback six months from now.

fastapi[standard]==0.115.*
sqlalchemy==2.0.*
pydantic[email]==2.9.*
uvicorn[standard]==0.32.*

# --- development / CI only (install with: pip install -r requirements-dev.txt)
# pytest==8.3.*
# httpx==0.27.*
